# Training-phase dependencies. Quantization/serving/agent phases will
# add their own requirements files as those parts get built.
torch>=2.5
transformers>=4.57
accelerate>=1.5
peft>=0.14
trl>=0.19          # need >=0.19 for native {"messages": ..., "tools": ...} SFT support
datasets>=3.0
pyyaml>=6.0
wandb>=0.18

# Flash Attention 2 is optional. PyTorch uses built-in SDPA by default.
# To optionally install flash-attn after torch is installed:
#   pip install flash-attn --no-build-isolation

# NOTE: trl moves fast and has broken backward compatibility on
# SFTConfig kwargs more than once (e.g. max_seq_length -> max_length).
# Run `pip install -U trl` and check
# https://huggingface.co/docs/trl/sft_trainer if training errors on
# an unrecognized argument.
